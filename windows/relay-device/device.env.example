# relay-device —— 接入自建 relay（mcp.example.com）的 device 配置
# ===========================================================================
# 复制成 device.env 后按需修改。device.env 不入库（含部署相关的本机路径）。
#
# 和 ../server.env 的区别：那份走 OpenAI 托管隧道（tunnel-client），
# 这份走自建 relay（Supabase + device OAuth）。两套互不干扰，可以并存。
#
# 除 MCP_SERVER_URL 外都是可选的 —— 留空/删掉就用下面注释里写的默认值。
# MCP_SERVER_URL 是**必填**：没设会直接报错退出（不会退回到某个地址再报 DNS 错）。

# 中继地址（**必填**）。设备 OAuth（/device/start、/device/poll）和 Supabase
# Realtime 的地址都由它推导，所以改这一个就够。
MCP_SERVER_URL=https://mcp.example.com

# node.exe 路径。一般不用设：脚本先找
#   %ProgramFiles%\nodejs\node.exe
#   %LOCALAPPDATA%\Programs\nodejs\node.exe
# 再从 PATH 里找。
# DCD_NODE_EXE=C:\Program Files\nodejs\node.exe

# device 私有副本的根目录。默认是脚本同级的 _device\（约 200MB，
# 全部内容都是本机复制，零联网）。
# 换到别处也行，但**不要**指向 npx 缓存目录本身 —— 那正是要隔离的东西。
# 路径别太深：Windows 长路径 260 字符，node_modules 里有很深的树。
# DCD_DEVICE_ROOT=C:\relay-device

# 从哪份现成的 node_modules 复制。默认自动扫描
#   %LOCALAPPDATA%\npm-cache\_npx\*\node_modules
# 取版本最高的那一份。只有自动扫描找不到时才需要手动指。
# DCD_SOURCE_NODE_MODULES=C:\Users\longyuan\AppData\Local\npm-cache\_npx\4b4c857f6efdfb61\node_modules
